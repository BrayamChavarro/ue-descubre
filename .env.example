# Example environment file (DO NOT USE IN PRODUCTION AS-IS)
# Copy this to a local file named `.env` (which is gitignored) and fill real secrets.
# Never commit your real `.env` with credentials.

# MongoDB Atlas connection string (replace <user> and <password>)
MONGODB_URI=mongodb+srv://<user>:<password>@clusterclientessedes.udohouw.mongodb.net/uempresarial?retryWrites=true&w=majority&appName=ClusterClientesSedes
DB_NAME=uempresarial

# Session / JWT secret (must be long & random in production)
SESSION_SECRET=change_this_secret

# Default admin bootstrap (change after first deploy)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_this_admin_password

# Runtime environment
NODE_ENV=development

# Mongo connection retry strategy
MONGO_CONNECT_RETRIES=8
MONGO_CONNECT_DELAY_MS=2500
